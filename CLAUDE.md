# AXTYPE

AX 타입 테스트. 정적 사이트, 빌드 도구 없음, 의존성 없음. 사람용 안내는 [README](./README.md), 설계 근거는 [docs/DESIGN.md](./docs/DESIGN.md).

## 철학 (paperthin 에서 빌림)

- **결과물을 믿지, 만든 사람을 믿지 않는다.** 문서와 화면은 이 세션을 못 본 사람에게도 읽혀야 한다. 바꾼 뒤엔 `sip` 을 돌려 `shower`(콜드 리드)로 확인한다.
- **한 사실은 한 곳에.** 타입·문항·공유 문구는 `data.js` 에만 산다. `r/` 와 `og/` 는 생성물이라 손으로 고치지 않는다. `scripts/check.mjs` 가 어긋남을 잡는다.
- **덜어내기.** 고칠 게 없으면 아무것도 바꾸지 않는다. 기능을 붙이기 전에 무엇을 뺄지 먼저 본다. 이메일 수집, 로그인, 분석 스크립트, 프레임워크는 의도적으로 없다.
- **정직.** 통계·출처를 지어내지 않는다. 근거 없는 "상위 3%" 같은 문구는 쓰지 않는다. 검증 가능한 것만 말한다 (`factchk`).
- **예시는 일반형으로.** 문서에 실제 PR 번호·핸들·날짜를 박지 않는다. 시간이 지나면 거짓이 된다.
- **버린 것도 기록.** 기각한 대안은 지우지 않고 `findings.md` 에 이유와 함께 남긴다.
- **거울은 원본이 아니다.** `design/` 의 아트보드는 화면을 옮긴 사본이다. 디자인 캔버스에서 바뀐 것은 `style.css`·`data.js` 로 되돌려 넣고, 그 뒤 아트보드를 다시 맞춘다. 반대 방향은 없다.

## 작업 기록 (3-file)

| 파일 | 역할 | 갱신 시점 |
|---|---|---|
| `tasks.md` | 목표, 단계, 결정, 오류 | 단계가 바뀔 때 |
| `findings.md` | 조사 결과, 기술 결정, 기각한 대안 | 알게 된 즉시 |
| `progress.md` | 세션별 작업 내역, 테스트 결과 | 세션마다 |

작업 시작 전에 `tasks.md` 의 Current Phase 와 `progress.md` 의 5-Question Reboot Check 를 읽는다.

## 명령

```bash
npm run dev     # 로컬 서버
npm run check   # 구조·균형·분포·생성물 동기화 검증. 문항을 바꿨으면 반드시.
npm run build   # r/, og/ 재생성 + check
```

## 규칙

- 타입의 `code` 는 대문자 4글자이고 첫 글자가 타입마다 달라야 한다. 결과 코드 `PRTO-G` 의 규칙은 `data.js` 의 `resultCode` 한 곳에 있다.
- `sw.js` 의 프리캐시 목록과 `manifest.webmanifest` 의 아이콘은 `check` 가 실제 파일과 대조한다. 정적 파일을 추가하면 목록과 `VERSION` 을 같이 올린다.
- 문항은 10개, 선택지는 3개, 각 선택지는 primary +2 / secondary +1. 모든 타입이 primary 6회, secondary 6회. `check` 가 강제한다.
- `assets/<id>.webp` 는 타입별 캐릭터 아트다. 파일명은 타입 id, 배경색은 `data.js` 의 `color` 와 같아야 한다 (`check` 가 존재만 확인하고 색은 사람이 맞춘다). 생성 프롬프트와 모델은 `findings.md` 에 있다.
- `SITE.url` 이 배포 주소의 유일한 원천이다 (끝 슬래시 없이). 런타임 공유 링크는 `location` 에서 계산하므로 어느 호스트에서도 동작하지만, 크롤러가 읽는 OG 절대 경로는 빌드 시점의 `SITE.url` 을 쓴다.
- 결과 문구는 정체성 언어("~하는 사람")로, 칭찬하되 정직하게. 평가 언어("당신은 ~가 부족합니다")는 쓰지 않는다.
- 커밋 메시지는 제목 한 줄, 빈 줄, 실제 변경 하나당 `-` 한 줄. diff 가 이미 증명하는 건 적지 않는다.

## 스킬

`.claude/skills/` 에 [paperthin](https://github.com/LilMGenius/paperthin) 전체가 설치되어 있다. 자주 쓰는 것: `readchk`(요청 읽기 확인), `sip`(만든 직후 자체 검수), `shower`(콜드 리드), `factchk`(사실 확인), `re0`(문서 정리), `ssotize`(중복 통합). 사용자 전용 스킬(`hate`, `prism`, `re0-git` 등)은 사람이 직접 부른다.
